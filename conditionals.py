# If/ Else conditions are used to decide to do something based on something being true or false

x_fba = 3
y_fba = 5

# Comparison Operators (==, !=, >, <, >=, <=) - Used to compare values

if x_fba > y_fba:
    print(f'{x_fba} is bigger than {y_fba}')



    if x_fba > y_fba:
         print(f'{x_fba} is bigger than {y_fba}')
    else:
         print(f'{y_fba} is bigger than {x_fba}')


          if x_fba > y_fba:
         print(f'{x_fba} is bigger than {y_fba}')
         elif x==y:
             print(f'{x_fba} is equal to 10{y_fba}')
    else:
         print(f'{y_fba} is bigger than {x_fba}')

if x_fba > 2:
    if x_fba <=10
        print(f'{x_fba} is greater than 2 and less than or equal to 10')

   # Logical operators (and, or, not) - Used to combine conditional statements


if x_fba>2 and x_fba<=10
  print(f'{x_fba} is greater than 2 and less than or equal to 10')
if x_fba>2 or x_fba<=10
    print(f'{x_fba} is greater than 2 or less than or equal to 10')

if not(x_fba==y):
    print(f'{x_fba}is not equal to {y}')






# Membership Operators (in, not in) - Membership operators are used to test if a sequence is presented in an object


numbers_fba = [1, 2, 3, 4, 5]
if x_fba in numbers_fba:
    print(x_fba in numbers_fba)
if x_fba not in numbers_fba:
    print(x_fba not in numbers_fba)




# Identity Operators (is, is not) - Compare the objects, not if they are equal, but if they are actually the same object, with the same memory location:
if x_fba is y_fba:
 print(x_fba is y_fba)

 if x_fba is not y_fba:
 print(x_fba is not y_fba)

